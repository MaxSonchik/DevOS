{
    // Используйте IntelliSense, чтобы узнать о возможных атрибутах.
    // Наведите указатель мыши, чтобы просмотреть описания существующих атрибутов.
    // Для получения дополнительной информации посетите: https://go.dev/doc/debugger
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch d-recon",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/d-recon.go",
            "args": [
                // === Здесь указываются аргументы командной строки ===
                "-t", "test.com",               // Цель для сканирования
                "--modules", "subdomains,ports", // Запуск только двух модулей
                "--verbose",                    // Подробный вывод
                "--timeout", "300"              // Увеличенный таймаут
            ]
        }
    ]
}